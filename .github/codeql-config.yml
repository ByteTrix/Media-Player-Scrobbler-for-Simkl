name: "CodeQL Config"

queries:
  - uses: security-and-quality
  - uses: security-extended

paths:
  - simkl_scrobbler
  - tests

paths-ignore:
  - docs
  - '**/*.md'
  - '**/*.txt'

query-filters:
  - exclude:
      id: py/empty-except-block
