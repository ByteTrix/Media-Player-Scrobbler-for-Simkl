# Simkl API Access Token (obtain from Simkl settings)
# This is used for authenticated requests after the initial OAuth flow.
SIMKL_ACCESS_TOKEN=YOUR_SIMKL_ACCESS_TOKEN_HERE

# Note: SIMKL_CLIENT_ID and SIMKL_CLIENT_SECRET should now be set as environment variables
# or injected during the build process (e.g., in GitHub Actions secrets).